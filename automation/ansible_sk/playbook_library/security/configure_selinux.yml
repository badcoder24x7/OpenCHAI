---
- name: Manage SELinux configuration
  hosts: all
  become: true
  vars_files:
    - ../../group_vars/all.yml
  vars:
    selinux_mode: disabled        # enforcing, permissive, disabled
    selinux_install_if_missing: true


  roles:
    - "{{ roles_parent_path }}/security_lib/manage_selinux"
