[Unit]
Description=Machine Check Exception Log Daemon (Foreground)
After=network.target

[Service]
Type=simple
ExecStart=/usr/sbin/mcelog --foreground --logfile={{ mcelog_log_file }}
Restart=always

[Install]
WantedBy=multi-user.target
