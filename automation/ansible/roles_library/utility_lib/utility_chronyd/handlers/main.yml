---
# ============================================================
# Chrony Handlers
# ============================================================

- name: Restart Chrony
  ansible.builtin.service:
    name: "{{ chrony_service_name }}"
    state: restarted
