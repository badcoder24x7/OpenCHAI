# Copy to .env.local and adjust as needed
# Base URL for REST API calls (proxied via Vite in dev, set explicitly in prod)
VITE_API_BASE=/api

# WebSocket base URL — leave blank to auto-detect from window.location
# VITE_WS_BASE=ws://your-head-node:8000/ws
